{
  "name": "vehicle-builder",
  "version": "1.0.0",
  "description": "TypeScript command-line app i order to build and use vehicle info",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "npx tsc",
    "start": "npm run build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "MC",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^9.2.19",
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.7",
    "@types/node": "^20.12.7"
  }
}
